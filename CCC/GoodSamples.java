import java.util.ArrayList;
import java.util.Scanner;

public class GoodSamples {
    
    
    public static void main(String[] args) { 
        Scanner in = new Scanner(System.in); 

        int numNotes = Integer.parseInt(in.next()); 
        int highestPitch = Integer.parseInt(in.next()); 
        long numGoodSamples = Long.parseLong(in.next()); 

        ArrayList<Integer> ansNoteSeq = new ArrayList<Integer>();
        
        for (int i = 0; i < numNotes; i++) { 

            // Check for non-distinct sample number 
            
            
        }
    }
}
